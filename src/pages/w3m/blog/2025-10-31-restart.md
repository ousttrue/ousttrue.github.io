---
title: 再スタート
date: 2025-10-31
tags: [w3m]
---

またもやりなおし。
だいぶ課題が整理されてきた。
そろそろうまくいくといいのだが。

なんだかんだで `w3m` を代替するツールは出てこない。
Cursor のある Pager という落としどころが良いのだと思う。
たいがいの Pager はカーソルが無いのだが、
カーソルがあると focus して action を起せるから機能が広がるのだ。

改造する上で課題となる点は以下のようなところだ。

# Bohem GC

Bohem GC で文字列などを管理している。
当初 c++ 化して `std::string` に置き換えられるか試みていたのだが
、これは筋が良くなかった。
今回は zig やるつもりで、c++ はやらない意向。

文字列は以下のような型になっている。

```c title="Str.h"
typedef struct _Str {
    char* ptr;
    int length;
    int area_size;
}* Str;
```

今回は、ここから手を付けてみようかと。
GC を zig の allocator として管理してみようと考えている。
そこを足がかりに、すこしずつ動く状態を維持して zig 化しようと企んでいる。

- [x] Str.c を zig 化できた

# zig 化

zig 化すると C要素へのアクセスが module 経由になる都合上、
global 変数や、macro へのアクセスも明示的になるので、
整理作業上すこぶる便利だということがわかった。
ファイル単位でやると量的に大変になったりするが、
関数単位で一個ずつ zig 化するのはそれほど大変ではない。

test を本文中に書けるのも、C移植がうまくいっているか確認できるので非常に良い。

# lua 化

さらに lua の導入する。
ziglua の導入が簡単だった。
もともと Commmand 定義を lua 化しようと思っていたのだが、
entry point と各種 設定ファイルのロード mainloop くらいまで lua 化してしまって、
ある程度 lua の debugger で確認できるところまでやってみようと思った。
